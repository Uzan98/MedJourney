{
  "buildCommand": "chmod +x ./setup-vercel.sh && ./setup-vercel.sh && NODE_OPTIONS=--max_old_space_size=4096 npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": ".next"
} 